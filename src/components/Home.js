import React from 'react';
import { Container, Typography } from '@mui/material';

const Home = () => {
  return <Container>
    <Typography variant='h3'>
      Homepage
    </Typography>
  </Container>;
};

export default Home;
